{
  "name": "chrome-action-recorder",
  "version": "1.0.0",
  "description": "Chrome extension to record user actions on websites",
  "scripts": {
    "build": "tsc && cp -r static/* dist/",
    "watch": "tsc --watch"
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.315",
    "typescript": "^5.3.3"
  }
}
